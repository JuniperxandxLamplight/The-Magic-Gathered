export class FilterButton {
  value: boolean = false;
  constructor(public name: string, public image: string, public id: number) {}
}

export class nameAndSuper {
  constructor(public name: string, public value: boolean, public id: number) {}
}
